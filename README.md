# `docker-barnard59-carml`

This Docker image contains the Carml JAR, NodeJS and `barnard59`.

To pull the Docker image, you can use the following command:

```sh
docker pull ghcr.io/zazukoians/barnard59-carml:latest
```
